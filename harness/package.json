{
  "name": "@forge-node/harness",
  "version": "1.0.0",
  "private": true,
  "description": "Human interface of the subagent harness (state on disk).",
  "type": "module",
  "bin": {
    "harness": "./src/cli.ts"
  },
  "scripts": {
    "harness": "bun run src/cli.ts",
    "validate": "bun run src/validate.ts",
    "typecheck": "bun --bun tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
