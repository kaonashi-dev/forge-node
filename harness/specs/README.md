*Each feature's specs live in `<id>-<slug>/` with requirements.md, design.md and tasks.md. The `spec-author` subagent writes them; do not edit them by hand except to correct the human gate. They are local state and gitignored: only this README is committed.*
